---
features:
  - |
    ``lapdog`` can instrument any Python application (or process that spawns a Python application). Install via ``pip install ddapm-test-agent[ddtrace]``, and run ``lapdog [your app command]``.
